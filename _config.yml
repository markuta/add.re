# Site settings
title: Add.re - MAC Address Search
tag_text: Add.re
description: Add.re is a website for Searching MAC addresses vendor details.

# Build settings
markdown: kramdown
exclude: ["README.md"]
sass:
  style: compressed
