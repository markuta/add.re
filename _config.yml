# Site settings
title: ADDR.re
tag_text: Solo
description: ADDR.re is a website for looking up MAC vendor details.

# Build settings
markdown: kramdown
exclude: ["README.md"]
sass:
  style: compressed
