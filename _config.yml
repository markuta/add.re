# Site settings
title: Addr.re
tag_text: Addr.re
description: Addr.re is a website for looking up MAC vendor details.

# Build settings
markdown: kramdown
exclude: ["README.md"]
sass:
  style: compressed
